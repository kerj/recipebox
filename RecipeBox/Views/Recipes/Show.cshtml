<h1>@Model["recipe"].Food</h1>

@if (@Model.Count != 0)
{
  <ul>
    @foreach (var recipe in @Model["recipe"])
    {
      <li>@recipe.Food</li>
      <li>@recipe.Instruction</li>
      <li>@recipe.Category</li>
    }
  </ul>
}
